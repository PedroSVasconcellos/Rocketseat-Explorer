//expression function

function createThink() {
  let subject = "study";
  return subject;
}

// ===========================

// arrow function

// (é igual, só tem um jeito diferente de escrever, suprimindo a palavra 'function' na hora de criar/declarar a função)

createThink = () => {
  let subject = "study";
  return subject;
}

