// manipulando Arrays

//transformar uma cadeia de caracteres em elementos separados de um array

// abaixo usando .split('')

let word = 'manipulação'
console.log(word.split(''))

// abaixo usando a função construtora(?) de Array acrescida do método Array.from()

console.log(Array.from(word))

// nenhum dos dois altera a estrutura do Array, só funciona em visualização(?)
